import { SetQuantityFormatterPipe } from './set-quantity-formatter.pipe';

describe('SetQuantityFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SetQuantityFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
